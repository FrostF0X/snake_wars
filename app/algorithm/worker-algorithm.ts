// Currently not used
/*
export class WorkerAlgorithm {
    constructor(algorithm: PlayerAlgorithm) {
        const worker: Worker = fork();
        worker.on("message", (boardJson: string) => {
            const board: string = JSON.parse(boardJson);
        });
    }
}
*/
